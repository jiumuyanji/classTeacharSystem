<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.classteather">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QueryStudentActivity"></activity>
        <activity android:name=".ReadvisitActivity" />
        <activity android:name=".WritevisitActivity" />
        <activity android:name=".VisitlistActivity" />
        <activity android:name=".ReadstudentActivity" />
        <activity android:name=".WritestudentActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".ZhucheActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>